(ns jaxank.template-test
  #_{:clj-kondo/ignore [:refer-all]}
  (:require [clojure.test :refer :all]
            [jaxank.template :refer :all]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 0))))
